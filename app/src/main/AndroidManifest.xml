<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.d3ifcool.aplikasi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/senarai_launcher_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/senaraiicon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListActivity" />
        <activity android:name=".Detail_ListActivity" />
        <activity
            android:name=".AddListActivity"
            android:label="LIST BARU" />
        <activity
            android:name=".todayActivity"
            android:label="TANGGAL" />
        <activity android:name=".SettingActivity" />
        <activity
            android:name=".helpActivity"
            android:label="@string/app_name"/>
        <activity android:name=".ProjectActivity" android:label="Projek"/>
    </application>

</manifest>